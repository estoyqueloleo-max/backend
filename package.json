{
	"name": "discover-api",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"//prebuild": "bash scripts/setup-tinygo.sh",
		"//build": "go run github.com/syumai/workers/cmd/workers-assets-gen && $(pwd)/_tinygo/bin/tinygo build -o ./build/app.wasm -target wasm -no-debug ./...",
		"build": "go run github.com/syumai/workers/cmd/workers-assets-gen -mode=go && sed -i '3i // Fix for Illegal invocation\\nif (globalThis.fetch) { globalThis.fetch = globalThis.fetch.bind(globalThis); }' ./build/worker.mjs && GOOS=js GOARCH=wasm go build -o ./build/app.wasm ./...",
		"deploy": "wrangler deploy",
		"dev": "wrangler dev",
		"start": "wrangler dev"
	},
	"devDependencies": {
		"wrangler": "^4.14.4"
	}
}
